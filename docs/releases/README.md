# Releases

## IronCrew

- [v0.4.5](v0.4.5.md) — Proton-Pass-Klartext im Legacy-Provider und portable Node-Auswahl für Testfixtures.
- [v0.4.4](v0.4.4.md) — Webstart aus versteckten Pfaden, kostenlose Live-Prüfungen, Antwortlimit und Proton-Pass-Kompatibilität.

- [v0.4.3](v0.4.3.md) — Modellläufe, private SQLite-Dateien, robuste Katalogabrufe, sichere Website-Artefakte und Nachtest-Korrekturen.

- [v0.4.2](v0.4.2.md) — große Kataloge atomar speichern, Proton-IDs korrekt übergeben, Speicherdiagnosen und Manifest-Schema 3.

- [v0.4.1](v0.4.1.md) — Linux-Korrekturen für Proton Pass, OpenRouter-Katalog, Bereitschaft und Einrichtung.
- [v0.4.0](v0.4.0.md) — neuer Produktkern im Workspace `next/`, veröffentlicht als Quellarchiv.

- [v0.3.1](v0.3.1.md) — Node.js 26, vollständige OpenRouter-Live-Auswahl, Deutsch/Englisch und kleinere Startbundles.

- [v0.3.0](v0.3.0.md) — aktive Firmenkonfiguration, Geschäftsdaten und objektive Modelltests.
- [v0.2.0](v0.2.0.md) — versionierte Firmenfreigaben und durchgängige Vendor-Durchsetzung.

- [v0.1.0](v0.1.0.md) — eigene Produktversionierung, gezielter Übergang von 2.8.0 und aktuelle Dokumentation mit Screenshots.
- [v2.8.0](v2.8.0.md) — historischer erster Release unter übernommener Versionsnummer.

[Manifestformat](source-manifest.md) · [Installation und Versionswechsel](../RELEASES.md) · [Dokumentationsindex](../README.md)

## Historische Entwicklungs- und Upstream-Hinweise

Die folgenden Nummern dokumentieren frühere Entwicklungsstände und sind keine
Aussage über aktuelle IronCrew-Releases oder heutige Provider-Policies.

These notes describe releases of
[OctoOffice](https://github.com/Chepko932/OctoOffice), the project IronCrew is
forked from, and are kept under the names those releases shipped with. They are
useful for the features IronCrew inherited; they are not IronCrew's own release
history, which starts after the fork.

- [`v2.6.0`](v2.6.0.md) - Code-review backlog cut: 41/41 high+critical issues closed across five waves (security/SSRF + DNS-rebind, a11y/WCAG, TS strictness, architecture refactors, +30 % backend tests). pnpm overrides for `ip-address`, `hono`, `fast-uri`. Backend coverage 35 → 39 % statements / 72 % functions; no breaking API changes
- [`v2.5.2`](v2.5.2.md) - Patch release: completed `pnpm setup` → `pnpm run setup` cleanup across CHANGELOG, release notes, AGENTS templates, setup scripts, and runtime routes (the bare `pnpm setup` command is reserved by pnpm itself); simplified `how-it-works.svg` to a single linear pipeline diagram
- [`v2.5.1`](v2.5.1.md) - Post-release hardening: mobile agent picker in chat panel, auth/CSRF fixes, dependency security patches (xmldom, hono, postcss), E2E test suite stabilisation, a11y and logging improvements, PPT workflow removal, community health files, neutral placeholder values across UI/tests
- [`v2.5.0`](v2.5.0.md) - OctoOffice rebrand (OctoOffice → OctoOffice, emerald palette, new logo), MissionControl 3-column Office home layout (AgentSidebarPanel + embedded Pixi canvas + MiniKanban + MetricsStrip + ChatPanel), OctoOfficeTopBar unified 46px navigation (7 tabs), LiveTaskView real-time agent activity + CLI terminal output panel, phase selection UI in task creation, pack department restructure, graph editor UX overhaul, autonomous scheduler safety gate (blocks tasks with pending-approval pipeline phases)
- [`v2.4.0`](v2.4.0.md) - Office Map v2 (7 offices, CEO seat, server room, hallway wandering, auto-pathfinding), dynamic pack inputs (PackInputsSection, UPDATE_INPUT, PackMetaPanel editable inputs tab), pack artifact collector for terminal phase outputs, decision inbox enriched with pack artifact context, worktree map rehydration on server startup, token usage tracking with per-task budget enforcement, pack task view improvements (task log fallback, diff modal portal), design system audit (90+ UI violations fixed), large file splits
- [`v2.3.0`](v2.3.0.md) - Visual Node Editor (Visualizer read-only DAG, Editor drag-to-connect + PropertyPanel + YAML preview, approval gate indicators), connector auto-dispatch for server-mode phases, phase reset API (single + BFS downstream), auto-workspace creation, ComfyUI TTS capability, MCP client integration (stdio/sse, auto-capability registration), API rate limiting, security hardening (path traversal, CSRF, token leak), observability stack (structured logging, workflow tracing, OTLP export), critical test coverage for core pipeline
- [`v2.2.0`](v2.2.0.md) - Platform architecture (declarative packs YAML + phase DAGs, connectors with capability routing, typed provider adapters, agent/hybrid/server execution modes), SSH server management + remote file browser, complete German UI translations, new vertical left-nav + Agent sidebar layout, Nefesh GTM community pack, dynamic pack registry endpoint, research pipeline improvements (#4), comprehensive CI test suite
- [`v2.1.0`](v2.1.0.md) - Agent ecosystem expansion (14 agents, 7 departments with default prompts), pixel-art character sprites + team-based office positioning, full mobile-responsive dashboard, retro UI overhaul (Press Start 2P), light/dark mode theming (CSS variables + localStorage), German language support, Obsidian Vault knowledge base integration, Projects view with GitHub PR + git status, OpenClaw CLI provider (per-agent `--profile` isolation), CEO autonomous orchestrator (inbox review, task creation, queue management, auto-approve)
- [`v2.0.1`](v2.0.1.md) - Discord bidirectional messenger stabilization release plus office-pack hydration-aware department merge precedence (pre-hydration pack profile, post-hydration DB), first-install bootstrap safety, AppMainLayout hydration-gate wiring, and regression tests for merge semantics/foreign-pack seed filtering
- [`v2.0.0`](v2.0.0.md) - Workflow Pack/Office Pack rollout release: pack-based orchestration surface (`development`/`report`/`web_research_report`/`novel`/`video_preprod`), header-integrated office-pack selector and isolated per-pack agent/department profiles, non-dev pack staff/department seed presets + avatar rendering consistency, multi-token messenger route isolation (`channel#tokenKey`) with Telegram per-token offsets, `/new` direct-chat session reset ACK flow, and decision notice readability v2 with concise planning summary + cleaner recommendation line
- `v1.2.3` - Direct messenger integration/stabilization release: unified built-in channel catalog + native adapters, Settings chat-session UX redesign (add/edit modal + immediate persist + agent avatar mapping + helper guidance), task-scoped channel relay isolation for report/chat/status_update, Decision Inbox in-messenger numeric reply flow with duplicate-send guard and compact formatting, messenger completion-report summary formatting with agent-identity intro, plus project-binding/path-safety hardening (`PROJECT_PATH_ALLOWED_ROOTS`) and direct-chat reliability/type cleanup
- `v1.2.2` - Interrupt-inject workflow rollout (new paused-session prompt injection endpoint + session-proof token + CSRF guard + terminal interrupt UX), plus Office Agent Detail per-agent CLI model overrides (`cli_model`) and Codex reasoning-effort override (`cli_reasoning_level`) propagated through task run/orchestration/spawn paths; includes schema/type expansion, API docs update for `/api/tasks/:id/inject`, light-mode interrupt contrast polish, and added interrupt control/injection tests
- `v1.2.1` - Post-v1.2.0 hardening release: core task-route split hotfix, CI Trojan Source (hidden/bidi Unicode) guard, pnpm version-mismatch fix, explicit typecheck/build gates with least-privilege workflow permissions, ESLint flat-config + CI lint baseline, staged incremental lint tightening (`lint-staged`), RuntimeContext/type-debt reduction (including Decision Inbox type dedupe), test-runtime DB isolation hardening, Swagger/OpenAPI docs exposure, OpenAPI contract automation (`openapi:sync`/`openapi:check` + CI gate + standard error/example coverage), and multilingual README/security/API docs refresh
- `v1.2.0` - Agent management CRUD (hire/edit/delete with full multilingual support), department CRUD (create/edit/delete with color/icon/prompt), department management tab with arrow/drag-and-drop sort order editing, new pixel character sprite (#13), project-level manual agent assignment with sprite avatar multi-select, meeting participant filtering for manual-mode projects (planning leader + assigned agents' department leaders only), task delegation manual-mode candidate restriction (assigned pool + same leader department), ChatPanel manual-mode indicator, mobile-responsive Project Manager (list-detail toggle), custom skill upload system (upload/list/delete with classroom training animation and light-mode support), bug fixes for runInTransaction 500 error, `/api/departments/reorder` route-collision save failure, light-mode error message contrast, and Agent Manager modal/emoji overflow scroll issues, plus post-release hardening kept in v1.2.0: manual-assignment safeguard confirmations/stats, backend `agent_ids` validation, delegation fallback audit/CEO notice, sprite registration collision guard, portable sprite generation output path, and department sort_order UNIQUE index safe migration
- `v1.1.9` - Post-v1.1.8 reliability hotfix bundle: sub-agent status-sync hardening (unknown `agent_status` live-sync fallback + Codex thread-binding TTL/size pruning and done-time cleanup), delegated review-gate pause/resume fixes (no false blocked finalization on pause interrupts, completion-time subtask reconciliation, stale blocked delegation auto-heal during review completion), and Decision Inbox review-round skip routing fix (runtime binding + rollback guard to prevent false project-level fallback)
- `v1.1.8` - Sub-agent orchestration reliability hardening across Claude/Codex/OpenCode/Gemini stream formats, burst-safe CLI output tail buffering for sub-agent UI visibility, equality/sync guardrails to reduce stale frames and ghost-agent artifacts, plus Office sub-agent visual refresh (random helper sprites, calmer support motion, smoke summon/despawn + fireworks) and Agent Detail sub-agent tab sprite-icon sync
- `v1.1.7` - OpenCode/Kimi stream-output hardening (final-reply extraction, raw JSON leak suppression, terminal-pretty reasoning visibility restore, explicit permission/stale-file/tool-calls/timeout error replies, improved OpenCode tool-progress hint stability), plus post-v1.1.6 hotfixes for custom API non-`v1` version-path compatibility and setup frontend URL port correction (`5173 -> 8800`)
- `v1.1.6` - GitHub project integration: dev-branch merge + automatic PR creation for GitHub-imported projects, `github_repo` column/API support, GitHub Import bugfixes, plus post-release hardening for subtask batch delegation worktree coverage across providers, shared HTTP fake-PID allocation, delegated-cancel queue safety, and pre-merge auto-commit policy/error classification improvements
- `v1.1.5` - Task/project creation UX overhaul: project-name input + click dropdown, inline new-project branch and one-flow project+task creation, desktop 1:1 expand + mobile scroll + outside-click close guard, latest-3 draft recovery submodals, project-path helper suite (auto/in-app/native picker) across Task + Project Manager, plus post-release hardening for helper API 404 compatibility, in-modal error UX, headless native-picker handling, duplicate `project_path` conflict prevention, allowed-root enforcement, backend `project_path` mapping fallback, and CLI spawn `ENOENT` resilience via cross-platform `PATH` fallback
- `v1.1.4` - Added global Auto Update toggle in Settings (server-wide), runtime toggle-apply endpoint (`/api/update-auto-config`), legacy upgrade migration keys (`autoUpdateEnabled`, `autoUpdateNoticePending`) with one-time notice for upgraded `v1.1.3` users, and fail-closed guard hardening with non-overridable `channel_check_unavailable`; updated multilingual READMEs and release docs
- `v1.1.3` - Project-first directive/task flow (existing/new project branch + top-10 selection), Project Manager rollout with mapped task/report history, task-detail report modal improvements, strict OpenClaw AGENTS upgrade enforcement (`HTTP 428 agent_upgrade_required`), light-mode Project Manager button contrast fix, browser stability optimization (WebSocket broadcast batching, tab-hidden polling pause, Pixi.js GPU memory cleanup, state array GC reduction, ChatPanel memoization), task creation agent assignment fix, run-guard UX with shake animation, dashboard header button primary/secondary redesign, and PR #23 (`SJY0917032`) follow-up for meeting-prompt/token compaction defaults (`MEETING_TRANSCRIPT_MAX_TURNS=20`) plus first-run `.env` auto-seeding for missing meeting prompt keys; added Active Agents `Script`/`Idle CLI` process-check menu with sample images
- `v1.1.2` - Dark/light theme-system rollout with tokenized UI surfaces, office scene palette adaptation, dashboard/sidebar/terminal readability polish, and light-mode learn/unlearn modal contrast fixes (including text-only note for updated Office screenshot asset)
- `v1.1.1` - Office Manager rollout (department/CEO/break-room theme control + live room highlight), tone-first preset behavior, report document pagination, DB-backed room-theme defaults/persistence, plus hotfixes for false `in_progress -> inbox` watchdog recovery and `/api/tasks` request burst throttling
- `v1.1.0` - PR #19 integration and release consolidation for Task Board bulk hide controls (Done/Pending/Cancelled), Active/All visibility toggle, Skills Learn/Unlearn UX upgrades, cross-platform unlearn verification, and post-release in-app update banner guidance
- `v1.0.9` - Report-request/PPT workflow hardening (HTML-first + one-pass design checkpoint + final regen/no second review), terminal live-hint UX and parser improvements, task log append retention, and multilingual README/report-doc updates
- `v1.0.8` - Active agents status panel + kill control, task completion report popup/history, planning-lead consolidated archive generation, long-run stability updates (`--max-turns 200`, timeout/orphan handling), and CLI/OAuth/API communication QA test suite
- `v1.0.7` - External API provider system (Settings API tab, CRUD + connection test), direct chat streaming for API/OAuth agents (`chat_stream` WS event), API provider task execution, `orchestration.ts` const crash fix, model caching layer (SQLite + memory 2-tier)
- `v1.0.6` - Windows cross-platform compatibility (CLI detection `shell: true`, Gemini version detection, skill-learn `spawn` fix), OAuth model provider independence (Copilot API direct fetch → 3-tier resolution), Settings panel version-display fallback, non-interactive esbuild build approval
- `v1.0.5` - Runtime modularization follow-up (route/workflow decomposition), `/api/inbox` security-doc parity (`x-inbox-secret`), one-time post-pull auto-migration (`INBOX_WEBHOOK_SECRET`/AGENTS/OpenClaw path), AGENTS secret-discovery hardening, runtime language-alignment fixes, long chat-response truncation/repetition fixes, workflow crash hotfix, and Skills Library Learn Squad background install flow (`npx skills add ...`)
- `v1.0.4` - Review flow stabilization (R1 single remediation batch -> R2 merge synthesis -> R3 final), planning-leader subtask rerouting, department-batched sequential checklist delegation, collaboration child review-gate + single parent finalization, orphan in-progress watchdog recovery, explicit remediation/collaboration reporting, and pause/resume session continuity hardening
- `v1.0.3` - Message delivery resilience hardening (idempotency/retry/audit verification) + review workflow guardrails for loop control
- `v1.0.2` - Agent token optimization: auto project context, recent changes sharing, CLAUDE.md generation, shared MVP code review policy + explicit warning-fix override
- `v1.0.1` - OAuth multi-account pool, Office account routing, direct chat-to-task execution, mobile virtual controls
