[Phase: Brief — Design Direction]

You are creating the design brief that will guide all subsequent design work.

1. Analyze the design goal, target surface, and brand constraints.
2. If a Figma URL is provided and API access is available, parse existing design context.
3. Define:
   - Target users and their primary tasks on this surface
   - Design principles and constraints (brand colors, typography, spacing rules)
   - Success metrics (what does "good" look like?)
   - Scope boundaries (what is NOT in scope)
4. Document any existing components or patterns that must be reused.

Save design brief to design_output/design_brief.md with sections:
## Goal, ## Target Users, ## Design Principles, ## Scope, ## Existing Patterns

Save requirements to design_output/requirements.json:
{ "surfaces": [...], "constraints": {...}, "success_metrics": [...], "reuse_components": [...] }

This phase requires user approval before proceeding.
