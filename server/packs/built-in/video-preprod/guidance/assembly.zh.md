[Phase: Assembly — 最终组装]
- 仅使用 video_output/clips/ 中的视频片段(.mp4)。
- 重要: 不要从静态图片创建片段（禁止Ken Burns、缩放、平移等效果）。Phase 5已经创建了片段 — 你的任务仅是拼接它们。
- 如果 video_output/clips/ 中没有片段，请停止并报告问题。不要用图片替代。
- 按shot_list.json定义的顺序组合片段。片段时长可能不同（每个3-10秒）。
- 使用ffmpeg连接片段并添加转场效果。
- 重要: 输出分辨率必须为720x1280（竖屏/9:16格式）。不要转换为横屏或改变宽高比。
- 如有配音脚本则参考（音频可单独添加）。
- 将最终视频保存到 video_output/final.mp4。
- 完成后验证文件大小和时长，并包含在报告中。
