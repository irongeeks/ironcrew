node_modules
dist
coverage
test-results
playwright-report
pnpm-lock.yaml
ironcrew.sqlite
ironcrew.sqlite-shm
ironcrew.sqlite-wal
# Pre-rename names, still present in checkouts that predate IronCrew.
octooffice.sqlite
octooffice.sqlite-shm
octooffice.sqlite-wal
logs
.ironcrew
.ironcrew-worktrees
.octooffice
.octooffice-worktrees
tools/playwright-mcp
tools/ppt_team_agent
docs/reports/Sample_Slides

# Generated by `scripts/setup.mjs` from templates/AGENTS-ironcrew.md, which
# rewrites the whole file on every run and does not format what it writes.
# Format-checking a file a script regenerates is a race the script always
# wins — the template is formatted instead, which is the copy a human edits.
AGENTS.md
